apiVersion: cert-manager.io/v1alpha2
kind: Certificate
metadata:
  name: jobflow-serving-cert
  namespace: kube-system
spec:
  dnsNames:
  - jobflow-webhook-service.kube-system.svc
  - jobflow-webhook-service.kube-system.svc.cluster.local
  issuerRef:
    kind: Issuer
    name: jobflow-selfsigned-issuer
  secretName: webhook-server-cert
